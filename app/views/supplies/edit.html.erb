<h1>Editing supply for <%=h @depot.name %></h1>

<% form_for(@supply) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => "form", :locals => { :f => f } %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Back', @supply %>