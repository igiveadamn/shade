<h1>Editing food_provider</h1>

<% form_for :food_provider, :url => admin_food_provider_path(@food_provider), :html => { :method => :put } do |f| %>
  <%= f.error_messages %>

  <p>
		<%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', admin_food_provider_path(@food_provider) %> |
<%= link_to 'Back', admin_food_providers_path %>