<h2>Users</h2>

<table>
	<tr>
		<th>Login</th>
		<th>Email</th>
	</tr>
	<% @users.each do |user| -%>
		<tr>
			<td><%= user.login %></td>
			<td><%= user.email %></td>
		</tr>
	<% end -%>
</table>

<div class="actions">
	<%= link_to "Create user", new_admin_user_path %>
</div>