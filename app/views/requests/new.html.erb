<h1>New request for <%=h @location.name %></h1>

<% form_for(@request) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', location_path(@location) %>